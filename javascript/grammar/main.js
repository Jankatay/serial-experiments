// comments -> syntax is c-like and '//' is recommended
//  /* var rm_a = /a*/.match(s); */

// numbers are internally 64-bit floating point by default
// invalid numbers are NaN, NaN != NaN and should use isNaN() function
// also you can use exponent notation and HUGE numbers become "Infinity"
// var huge = 9e9999999999999999999999999

// There are no "char" type, only string. char is just a one-element string
// strings are also immutable and all have "str.length." 
// "\u" is used for giving unicode values as is, all chars are 16-bits each
// 'A' === '\u0041'
